sending an ajax request to a cgi script via js:
$(function(){
    $.ajax({
    url: "ServerAdress/cgi-bin/" + service.py,
    type: "POST",
    async:true,
    data: {key_vehicule: 'mercedes'},
    success: function(response){

        },
    error: function(){

        },
    complete: function(){

        }
    });

JS prog reference: https://github.com/getify/You-Dont-Know-JS/blob/master/README.md
Console wiz: https://blog.teamtreehouse.com/mastering-developer-tools-console

<textarea id = "" rows = "5" cols = "80"></textarea>

Enter trigger: .addEventListener('keyup', function(event) {
    if (e.keyCode === 13) { // checks for enter key
        doSomething();
    }
})

<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>

getting images from google. Details change all the time tho
urls=Array.from(document.querySelectorAll('.rg_i')).map(el=> el.hasAttribute('data-src')?el.getAttribute('data-src'):el.getAttribute('data-iurl'));
window.open('data:text/csv;charset=utf-8,' + escape(urls.join('\n')));
copy(Array.from(document.querySelectorAll('.rg_i')).map(el=> el.getAttribute('data-iurl')).join("\n"))
copy(Array.from(document.querySelectorAll('.rg_i')).map(el=> el.getAttribute('src')).join("\n"))
.rg_meta
.rg_di
working one at time of writing: copy(Array.from(document.querySelectorAll('.rg_i')).map(el=> el.hasAttribute('data-src')?el.getAttribute('data-src'):el.getAttribute('data-iurl')).join("\n"))
