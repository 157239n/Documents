
conda activate
conda deactivate

conda list: list all installed packages
conda create --name myenv
conda install {package name}
conda install -n myenv scipy=0.15.0 astroid
conda search {package name}

conda env create -f environment.yml
conda env list
conda create --name myclone --clone myenv
conda info --envs
conda info

anaconda-navigator

conda config --show channels
conda config --add channels bioconda

setting envs when activate an environment: https://stackoverflow.com/questions/46826497/conda-set-ld-library-path-for-env-only

building c libraries: https://docs.conda.io/projects/conda-build/en/latest/
for a more popular example, see cython-feedstock
